/**
 * These rules relate to using strict mode.
 * @module rules/strict-mode
 * @see http://eslint.org/docs/rules/#strict-mode
 */

'use strict';

module.exports = {
  rules: {
    // Controls location of Use Strict Directives
    strict: [2, 'global']
  }
};
